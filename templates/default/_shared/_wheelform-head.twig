{% if wheelform is defined and wheelform.getSettings('recaptcha_public')|length and getenv('RECAPTCHA_PUBLIC_KEY')|length %}
  <meta name="invisible-recaptcha-sitekey" content="{{ getenv('RECAPTCHA_PUBLIC_KEY') }}">
{% endif %}
